rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // All authenticated users can read/write ANY collection/document
    // in the Firestore. 
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
